#include <stdlib.h>
#include <stdio.h>
main ()

{      int A, B, C, D, prod, DIFERENCA;

       scanf("%i", &A);
       scanf("%i", &B);
       scanf("%i", &C);
       scanf("%i", &D);
       
       DIFERENCA = (A*B) - (C*D);
       
       printf("DIFERENCA = %i\n", DIFERENCA);
       
       system ("PAUSE");
       
    }
